auth_modules:
  "192.168.1.161:5432/cht": # Set this to any name you want
    type: userpass
    userpass:
      username: postgres_root
      password: postgres_root_password
    options:
      # options become key=value parameters of the DSN
      sslmode: disable
