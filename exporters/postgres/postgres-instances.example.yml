- targets:
    # Update this URL to point to your Postgres DB endpoint.
    # Add additional URLs to monitor multiple instances.
    - 192.168.1.161:5432/cht
  labels:
    cht_instance: fake-cht:8081
